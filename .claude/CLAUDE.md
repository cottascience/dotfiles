# Shell commands

Prefix every shell command with `rtk`.

The `PreToolUse` hook also rewrites supported Bash commands through RTK. Use
`rtk proxy <command>` only when you need unfiltered output.

Useful diagnostics: `rtk --version`, `rtk gain`, and `rtk gain --history`.
